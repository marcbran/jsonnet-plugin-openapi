openapi: 3.0.3
info:
  title: Unsafe Jsonnet identifiers
  version: "1"
paths:
  /user-profiles:
    get:
      operationId: listUserProfiles
      parameters:
        - name: X-Trace-Id
          in: header
          required: false
          schema:
            type: string
      responses:
        "200":
          description: ok
